# AGENTS.md

<!-- Minimal context for an agent (or human) to be productive immediately on
     this repo. Keep it short, accurate, up to date: delete what becomes
     false rather than let stale info linger. Target: under 300 lines,
     ideally under 60. -->

## What this project does

<!-- WHY: purpose in 2-4 sentences. Deliberate scope, and what's explicitly
     out of scope (e.g. "don't add X without asking first"). -->

## Commands

<!-- HOW: commands an agent must use to install, run, test, lint/format.
     Mention any wrapper (Makefile, script) to use instead of raw tools. -->

```bash
# e.g.
# make install   # ...
# make test      # ...
# make lint      # ...
```

## Architecture

<!-- WHAT: map of the repo - key files/dirs and their responsibility.
     Especially critical for a monorepo. Prefer pointers (`file:line`) over
     duplicating detail that will go stale. If architecture is complex,
     summarize here and point to a progressive-disclosure file
     (e.g. agent_docs/architecture.md) for depth. -->

## Code conventions

<!-- Rules specific to this repo only (style, dependencies to avoid, module
     boundaries). Don't duplicate what an already-configured linter/formatter
     already enforces. -->

## Tests

<!-- Where tests live, how to run them, what must be tested (e.g. "every new
     route needs a test"). -->

## Known pitfalls

<!-- Surprising behavior, invariants to preserve, past mistakes. Revisit
     periodically: move anything too specific/situational to a
     progressive-disclosure file. -->

## Configuration

<!-- Optional. Env vars, config files, secrets - only what an agent needs to
     operate, not a full ops guide. -->
